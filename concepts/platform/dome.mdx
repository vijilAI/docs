---
title: "Dome"
description: "Runtime protection product that intercepts inputs and outputs through Guardrails built from Guards and Detectors."
---

- Introduce Dome as Vijil's runtime protection layer for production agents. It intercepts agent inputs and outputs in real time, filtering harmful content, detecting anomalies, and enforcing policies with sub-300ms latency. Mention the multi-layer approach: pattern matching, ML classifiers, embeddings, and LLM evaluation.

## Defense Components

- One short paragraph introducing the defense hierarchy — Guardrail → Guard → Detector — before the subsections.
- Cards for Defense Components

## Observability

- Describe what Dome exposes for monitoring production agents
- **Execution traces** — full traces of LLM and tool calls with guard results and latency
- **Metrics** — inputs/outputs scanned, block/redaction rates, guard-level trigger rates, p50/p99 latency
- **Logs** — detection logs (when guards trigger) and audit logs (for compliance)
- **Dashboard** — real-time overview with threat timeline and per-guard drill-down\]\*

## **Dome**

Dome is Vijil’s protection layer, basically the part of the platform that watches over your agent once it’s live in production.

Instead of testing behavior in advance (like Diamond), Dome works in real time. It sits between the user and the agent, intercepting both **inputs and outputs** as they happen. This allows Dome to filter harmful content, detect suspicious patterns, and strengthen policies without slowing things down (latency stays under ~300ms).

What makes Dome effective is its **multi-layer approach**. That means that Dome doesn’t rely on just one method, but combines these things:

- simple pattern matching
- machine learning classifiers
- embedding-based similarity checks
- and LLM-based evaluation

All of these components are working together just to catch different risks, whether they are some obvious types of policy violation or something that’s less obvious.

## **Defense Components**

Under the hood, Dome follows a clear structure for how protection is applied:

**Guardrail → Guard → Detector**

You don’t need to configure everything from scratch, but if you try to understand this hierarchy it makes it easier to see how decisions are made and where controls are applied.

### **Defense Components**

- **Guardrail**\
  A guardrail defines _what kind of behavior you want to control_. For example, blocking sensitive data, preventing prompt injection, or enforcing content policies.
- **Guard**\
  Guards are the building material inside a guardrail. Each guard is focused on a specific check or rule, such as scanning for secrets, toxic language, or unusual patterns.
- **Detector**\
  Detectors are the ones who actually evaluate the data. They analyze inputs and outputs and decide whether something should be marked, blocked, or modified.

When you put together these components, they allow Dome to apply protection.

## **Observability**

Dome doesn’t just protect your agent but it also makes it easier to understand what’s happening in production.

This gives teams clear overview into how the agent behaves, what gets flagged, and from where potential risks are coming.

Here’s what you can monitor everything:

- **Execution traces**\
  Full, step-by-step traces of LLM responses and tool calls, including which guards were triggered and how long each step took.
- **Metrics**\
  High-level numbers that show system behavior, like how many inputs and outputs were scanned, how often something was blocked or redacted, trigger rates per guard, and latency (p50/p99).
- **Logs**\
  Detailed records of events:
  - detection logs (when and why a guard was triggered)
  - audit logs (useful for compliance and reviews)
- **Dashboard**\
  A real-time view of everything happening in production. You can follow a threat timeline, see patterns over time, and get to the bottom of a specific guard to understand what’s going on.